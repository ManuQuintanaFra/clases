import { CopyClipBoard } from './copy-clip-board';

describe('CopyClipBoard', () => {
  it('should create an instance', () => {
    const directive = new CopyClipBoard();
    expect(directive).toBeTruthy();
  });
});
